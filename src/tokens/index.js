// Public API: components import only from here.
// Primitives layer is intentionally not re-exported — keep raw values out of component logic.
export { alias, component } from './semantic/colors';
export { spacing, typography, shape, elevation } from './primitives';
